import { CategorySelector } from './category-selector';

describe('CategorySelector', () => {
  it('should create an instance', () => {
    expect(new CategorySelector()).toBeTruthy();
  });
});
